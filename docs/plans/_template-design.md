# [功能名称] 策划文档

> **Status:** draft | review | approved | superseded  
> **实施计划：** （策划通过后创建）[`YYYY-MM-DD-<feature>.md`](YYYY-MM-DD-<feature>.md)

**For agentic workers:** 填完整文各章节；`Status: approved` 前不得创建实施计划、不得改业务代码。无把握处写「待定」并列入风险。

---

## 背景与目标

（解决什么问题；一期交付边界一句话 + 2–3 句展开）

---

## 用户场景

1. （典型流程 1：如登录 → 进场 → …）
2. （典型流程 2）
3. （典型流程 3，可选）

---

## 功能范围

### 包含

- （一期必做项）

### 不包含

- （明确不做，防 scope creep）

---

## 协议设计

| 类型 | Route / 事件 | 请求 | 响应 / Push | 说明 |
|------|----------------|------|-------------|------|
| RPC | `game.xxx.yyy` | `XxxRequest` | `XxxResponse` | |
| Push | `onXxxChange` | — | `XxxBroadcast` | |

**业务错误码草案：** `40xxx` — （含义）

**前置条件：** （如须 `enter`、Session 字段）

---

## 数据与持久化

| 表 / 缓存 | 字段 / Key | 索引 / TTL | 说明 |
|-----------|------------|------------|------|
| | | | |

**事务：** （是否 `WithinTx`、`AfterCommit` 刷缓存）

---

## 业务规则

- （不变量、堆叠/槽位/幂等等）
- （与现有模块的交互）

---

## 验收标准

- [ ] （可测试的完成定义 1）
- [ ] （完成定义 2）
- [ ] `go test ./...` 通过
- [ ] （可选）`client-demo` 冒烟

---

## 风险与依赖

| 项 | 说明 |
|----|------|
| 依赖 | （如 backlog-item-config） |
| 迁移 | （表结构变更、兼容 v1 数据） |
| 兼容 | （客户端/协议版本） |

---

## 实施索引

策划 **approved** 后：

1. 复制 [`_template.md`](_template.md) → `YYYY-MM-DD-<feature>.md`
2. 实施 plan 文首 **Design:** 链接本文件
3. 任务清单从本章「协议 / 数据 / 验收」派生，不重复粘贴策划正文
