# GM Web 控制台 实施计划

> **Status:** done  
> **Design:** [`2026-09-14-gm-web-ui-design.md`](2026-09-14-gm-web-ui-design.md)（**Status: approved**）  
> **For agentic workers:** 按任务勾选推进；完成后更新 `docs/plans/README.md` 总览表。

**硬性顺序：** 策划文档 `approved` 后方可创建本文件并写业务代码。任务从策划的协议/数据/验收派生，勿重复长篇策划正文。

**Goal:** Vue 3 控制台覆盖 P0 GM API，由 GM HTTP 同源托管。

**Architecture:** `web/gm-console` 构建到 `internal/gmapp/ui`，`go:embed` + SPA fallback；`/gm/*` 仍走 JSON。

**Tech Stack:** Vue 3 / Vite / Vue Router / Go embed / Docker Node 阶段

---

## 任务清单

### Task 1: GM 静态托管

**Files:** `internal/gmapp/ui.go`、`internal/gmapp/http.go`、`internal/gmapp/ui/index.html`、`internal/gmapp/http_test.go`

- [x] embed + `/` SPA fallback，不拦截 `/gm/*`
- [x] 单测：`/` HTML，`/player` fallback，`/gm/health` JSON

### Task 2: Vue 控制台

**Files:** `web/gm-console/**`

- [x] 登录 / 概览 / 查账号角色背包 / 发奖 / 踢人 / 热更
- [x] sessionStorage token；危险操作确认
- [x] `npm run build` 写入 `internal/gmapp/ui`

### Task 3: Docker 与文档

- [x] Dockerfile Node 构建阶段
- [x] README 控制台地址；gitignore `node_modules`

---

## 验证

- [x] `go test ./internal/gmapp/...`
- [x] `go test ./...`
- [x] `go build ./...`
