# 统一游戏时间（gtime）实施计划

> **Status:** done  
> **完成日期：** 2026-05-28

**Goal:** 业务逻辑统一使用 `gtime.Now()`，支持 Redis 时间偏置与日历/刷新点工具。

**Architecture:** 封装 Cherry `cherryTime` 偏移；`{prefix}:meta:time_bias` 存偏置秒；DB 模型 `CreatedAt` 仍用真实 `time.Time`。

**Tech Stack:** `internal/gtime` / Redis

---

## 任务清单

- [x] `clock.go` — Now / UnixNow / RealNow / SetBiasSeconds
- [x] `calendar.go` — ZeroHourUnix、IsOverDay、DiffDay 等
- [x] `bias_redis.go` — Load/Save Redis 偏置
- [x] `persistence/token.go` 改用 `gtime.Now()`
- [x] `store.Init` 加载偏置
- [x] 单元测试 + README「时间（gtime）」章节
