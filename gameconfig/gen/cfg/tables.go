// Code generated by gameconfig toolchain. DO NOT EDIT manually unless Luban is unavailable.

package cfg

// Tables 聚合所有配置表实例（Luban Tables 等价物）。
type Tables struct {
	TbItem *TbItem
}

// TbItem 道具配置表，按 id 索引。
type TbItem struct {
	data map[int32]*Item
}

// NewTbItem 从行列表构建索引表。
func NewTbItem(rows []*Item) *TbItem {
	m := make(map[int32]*Item, len(rows))
	for _, row := range rows {
		if row == nil {
			continue
		}
		m[row.ID] = row
	}
	return &TbItem{data: m}
}

// Get 按 id 查询；不存在时 ok=false。
func (t *TbItem) Get(id int32) (*Item, bool) {
	if t == nil {
		return nil, false
	}
	v, ok := t.data[id]
	return v, ok
}

// DataList 返回全部行（顺序不保证）。
func (t *TbItem) DataList() []*Item {
	if t == nil {
		return nil
	}
	out := make([]*Item, 0, len(t.data))
	for _, v := range t.data {
		out = append(out, v)
	}
	return out
}

// NewTables 组装 Tables。
func NewTables(tbItem *TbItem) *Tables {
	return &Tables{TbItem: tbItem}
}
